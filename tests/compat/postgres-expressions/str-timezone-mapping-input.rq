PREFIX dc: <http://purl.org/dc/elements/1.1/>
PREFIX ns: <http://example.org/ns#>

SELECT (STR(?year) AS ?value) WHERE {
  ?x ns:price ?price .
  ?x dc:title ?title .
  ?x ns:pubYear ?year .
  ?x ns:discount ?discount .
}
ORDER BY ?year
