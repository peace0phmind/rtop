PREFIX dc: <http://purl.org/dc/elements/1.1/>
PREFIX ns: <http://example.org/ns#>

SELECT ?bound WHERE {
  ?x ns:price ?price .
  ?x ns:discount ?discount .
  OPTIONAL {
    ?x dc:title ?title .
    FILTER(LANGMATCHES(LANG(?title), "EN"))
  }
  BIND(BOUND(?title) AS ?bound)
}
ORDER BY ?title
