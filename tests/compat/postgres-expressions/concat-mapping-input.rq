PREFIX dc: <http://purl.org/dc/elements/1.1/>
PREFIX ns: <http://example.org/ns#>

SELECT (CONCAT(?title, " | ", ?description) AS ?value) WHERE {
  ?x ns:price ?price .
  ?x dc:title ?title .
  ?x dc:description ?description .
  ?x ns:pubYear ?year .
  ?x ns:discount ?discount .
}
ORDER BY ?title
